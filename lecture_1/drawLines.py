import turtle 
paxsTurtle = turtle.Turtle()

def drawLines(angle, length, turtle):
    while
        turtle.goto(0, 0)
        turtle.pendown()
        turtle.forward(length)
        turtle.left(angle)


drawLines(2, 50, paxsTurtle)   
turtle.done()